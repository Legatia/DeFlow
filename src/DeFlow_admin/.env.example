# DeFlow Admin Environment Variables
# Copy this file to .env and fill in the actual values

# REQUIRED: Owner Principal (must be set for security)
VITE_OWNER_PRINCIPAL=your-principal-id-here

# REQUIRED: Canister IDs
VITE_CANISTER_ID_DEFLOW_POOL=your-pool-canister-id
VITE_CANISTER_ID_DEFLOW_BACKEND=your-backend-canister-id
VITE_CANISTER_ID_DEFLOW_FRONTEND=your-frontend-canister-id
VITE_CANISTER_ID_DEFLOW_ADMIN=your-admin-canister-id

# REQUIRED: Internet Identity Canister ID (for local development)
VITE_INTERNET_IDENTITY_CANISTER_ID=rdmx6-jaaaa-aaaah-qcaiq-cai

# Network Configuration (set by dfx)
DFX_NETWORK=local
CANISTER_CANDID_PATH=...
CANISTER_CANDID_PATH_DEFLOW_BACKEND=...
CANISTER_CANDID_PATH_DEFLOW_POOL=...

# Security Notes:
# 1. NEVER commit .env files to version control
# 2. The VITE_OWNER_PRINCIPAL is critical for admin access control
# 3. Use different principals for development and production
# 4. Regularly rotate access credentials